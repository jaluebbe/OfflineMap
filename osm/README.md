# OSM Data
 
Place regional MBTiles extracts in this directory.
 
Files named `osm_*.mbtiles` (e.g. `osm_niedersachsen.mbtiles`) will be listed
as selectable map regions via the `/api/vector/regions` endpoint.
